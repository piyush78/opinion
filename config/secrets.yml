# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  secret_key_base: 6ec7716eabb82a7065c7454c4211c5c9de6e4d836f3ad4f922f89674ec9c5802415af62c60fb5f881e33b8b68f7300df588b0348e1ee1a1e2625cd9e6727294d

test:
  secret_key_base: ccfdb0c51ce22218b22d238d31973df3cf522dbd8fb25a9e954e4daa8db38b155a96c6f6699e7f9fc6eaeae3790bb1e9074723e26f151bc649e6016ab2de359a

# Do not keep production secrets in the repository,
# instead read values from the environment.
# production:
#   secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>


staging:
 secret_key_base : <%= ENV["STAGING_SECRET_KEY_BASE" ] %>
production:
 secret_key_base : <%= ENV["PRODUCTION_SECRET_KEY_BASE" ] %>
